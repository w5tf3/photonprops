from setuptools import setup

setup(
      name='photonprops',
      version='0.0.1',
      install_requires=[
                        'numpy',
                        'matplotlib',
                        'tqdm',
                        'qutip'
                        ]
      )
